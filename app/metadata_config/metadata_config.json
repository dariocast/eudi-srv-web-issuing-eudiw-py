{
  "credential_issuer": "{{SERVICE_URL}}",
  "credential_endpoint": "{{SERVICE_URL}}/credential",
  "batch_credential_endpoint": "{{SERVICE_URL}}/batch_credential",
  "notification_endpoint": "{{SERVICE_URL}}/notification",
  "deferred_credential_endpoint": "{{SERVICE_URL}}/deferred_credential",
  "display": [
    {
    "name": "Infocert EUDI Credentials Issuer",
    "locale": "en",
    "logo": {
        "uri": "https://www.infocert.it/content/uploads/2021/09/logo.svg",
        "alt_text": "Infocert Logo"
    }
    }
  ],
  "credential_configurations_supported": {
    

  }
}